# Handover — เครื่องมือวิศวกรรม IAQ · Acoustic · ERV

อัปเดต 7 กันยายน 2569 · เอกสารนี้อยู่ใน repo เพื่อให้เข้าถึงได้โดยไม่ผูกกับบัญชีใด

> **คนที่จะ *ใช้* เครื่องมือ ให้เปิด [คู่มือการใช้งาน](https://nchatupongtrakul-ops.github.io/design-management-dashboard/guide/) แทน**
> เอกสารนี้เขียนสำหรับคนที่จะมา *ดูแลหรือแก้* เครื่องมือต่อ

---

## 1. สถานะโดยสรุป

งานมีสองส่วน คือ **เครื่องมือคำนวณที่ขึ้นเว็บแล้ว** (ใช้งานได้ทันที) และ
**งานออกแบบห้องสัมมนาดนตรี** (พักไว้ตามที่ตกลง)

| | สถานะ |
|---|---|
| **Project Hub** (ทะเบียนกลาง) | ออนไลน์ · เครื่องมือทุกตัวเขียนกลับมาที่นี่ |
| Ventilation & CO₂ Calculator | ออนไลน์ · ต่อกับ Hub แล้ว |
| ERV Load Impact Calculator | ออนไลน์ · ต่อกับ Hub แล้ว |
| Noise Breakout Calculator | ออนไลน์ · ต่อกับ Hub แล้ว · **ค่าลดเสียง 3 ชิ้นยังเป็นค่าประมาณ** |
| ERV Energy Saving Calculator | ออนไลน์ · คิด ROI ต่อโปรเจค · มีแท็บสไลด์สำหรับเสนอลูกค้า |
| คู่มือการใช้งาน | ออนไลน์ · เขียนใหม่รอบ Hub แล้ว |
| ERV Team Portal (เมนู Tools) | ออนไลน์ · ลงทะเบียนครบทั้งหกรายการ |
| ข้อเสนอ ERV ห้อง Music Hall | ร่างเสร็จ · **พักไว้** ตามที่ตกลงว่าจะเน้นเครื่องมือก่อน |

**สิ่งที่เปลี่ยนไปมากที่สุดตั้งแต่ฉบับก่อน** เดิมเครื่องมือแต่ละตัวต่างคนต่างอยู่
ปิดหน้าแล้วค่าหาย ตอนนี้มีทะเบียนกลางชุดเดียวใน `localStorage` คีย์ `coral.projects.v1`
ที่ทั้งห้าหน้าอ่านเขียนร่วมกัน · **โปรเจคหนึ่งมีหลายห้อง** และนั่นคือข้อจำกัดที่ทั้งระบบถูกออกแบบรอบมัน

---

## 2. ที่อยู่ของทุกอย่าง

| สิ่งที่ทำ | URL | Repo · โฟลเดอร์ |
|---|---|---|
| ERV Team Portal | https://nchatupongtrakul-ops.github.io/erv-team-portal/ | `erv-team-portal` · root |
| **Project Hub** | https://nchatupongtrakul-ops.github.io/design-management-dashboard/hub/ | `design-management-dashboard` · `hub/` |
| **คู่มือการใช้งาน** | https://nchatupongtrakul-ops.github.io/design-management-dashboard/guide/ | `design-management-dashboard` · `guide/` |
| Ventilation & CO₂ | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ventilation/ | `design-management-dashboard` · `ventilation/` |
| Noise Breakout | https://nchatupongtrakul-ops.github.io/design-management-dashboard/acoustic/ | `design-management-dashboard` · `acoustic/` |
| ERV Load Impact | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ervload/ | `design-management-dashboard` · `ervload/` |
| ERV Energy Saving | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ervroi/ | `design-management-dashboard` · `ervroi/` |
| Design Management Dashboard | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ | ของเดิม · **ไม่ได้แก้ และห้ามแก้** |
| Construction Dashboard | ของทีมอื่น | `wyuwachotiphan-crypto/CM-Dashborad` · **ไม่ได้แตะ** |

> repo `design-management-dashboard` มี commit `Update dashboard data` เข้ามาอัตโนมัติเป็นระยะ
> แตะเฉพาะ `data.json` — **ให้ `git pull` ก่อน push ทุกครั้ง**

---

## 3. เครื่องมือแต่ละตัวตอบอะไร

ทั้งชุดต่อกันเป็นสายเดียว ผลของขั้นก่อนหน้าเป็นอินพุตของขั้นถัดไป
โดยส่งผ่านทะเบียนกลาง ไม่ต้องจดเลขไปมา ขอแค่เข้าทาง Hub

```
0  Project Hub         →  สร้างโปรเจค ใส่ห้องให้ครบ พร้อมแอร์เดิมของแต่ละห้อง
1  Ventilation & CO2   →  ปริมาณลมที่ต้องเติม เป็น CMH        ทีละห้อง
2  ERV Load Impact     →  ผลกระทบต่อแอร์เดิม เป็น RT และ องศา  ทีละห้อง
3  Noise Breakout      →  ชุดลดเสียงที่ต้องมี                  ทีละห้อง
4  ERV Energy Saving   →  ประหยัดปีละเท่าไร คืนทุนกี่ปี        ทั้งโปรเจครวดเดียว

ถ้า 2 หรือ 3 ไม่ผ่าน  →  ย้อนกลับไป 1 ของห้องนั้น ลดลมลง แล้วไล่ใหม่
```

**ข้อ 4 ต้องรอให้ทุกห้องผ่านข้อ 1 ก่อน** ไม่งั้นลมรวมยังไม่ครบ
เงินที่ประหยัดจะต่ำกว่าจริงและจำนวนปีคืนทุนจะนานกว่าจริง
หน้า ERV Energy Saving เตือนไว้ใต้แถบเลื่อนลมแล้ว

### 3.0 Project Hub — ทะเบียนกลาง

เก็บ **โปรเจค → ห้อง → ผลของแต่ละเครื่องมือ** และบวกยอดรวมให้โดยไม่ต้องเปิดเครื่องมือ
สร้าง ลบ เปิด ปิด โปรเจคที่นี่ · แก้ชื่อห้อง ขนาด จำนวนคน และแอร์เดิมได้ในตารางตรง ๆ
· Export/Import CSV หนึ่งแถวต่อหนึ่งห้อง

**สิ่งที่ต้องเข้าใจก่อนแก้โค้ดใด ๆ ที่แตะทะเบียนนี้**

| ค่า | รวมทั้งโปรเจคได้ไหม |
|---|---|
| ลม CMH · ภาระ RT · พื้นที่ · จำนวนคน · ขนาดแอร์เดิม | บวกได้ |
| **CO₂ ppm** | **บวกไม่ได้ เฉลี่ยก็ไม่ได้** เป็นค่าของแต่ละห้อง |
| **เสียง dB(A)** | **บวกไม่ได้** ขึ้นกับช่องเปิดและระยะของห้องนั้น |
| ROI · คืนทุน | เป็นของทั้งโปรเจคอยู่แล้ว ไม่ได้คิดรายห้อง |

ในหน้า **ยอดรวมของโปรเจค** ค่าที่บวกไม่ได้จะขึ้นว่า *ดูรายห้อง* ไม่ใช่ตัวเลข
**นั่นตั้งใจ อย่า "แก้" ให้มันแสดงค่าเฉลี่ย**

### 3.1 Ventilation & CO₂

หาปริมาณลมบริสุทธิ์ที่ต้องเติม และทำนาย CO₂ ตลอดระยะเวลาใช้ห้อง
เทียบกับเกณฑ์กฎกระทรวงฉบับที่ 39 · ASHRAE 62.1 · LEED/WELL พร้อมกัน
มีเส้นเปรียบเทียบระบบเดิมของโครงการ และช่องย้อนกลับ (ตั้งเป้า ppm แล้วหาลม)

**ตารางข้อมูลที่ฝังไว้** — ASHRAE 62.1 Table 6-1 จำนวน 108 หมวด · อัตราปล่อย CO₂ 32 กิจกรรม ·
กฎกระทรวงทั้งสองชุด อยู่ในตัวแปร `CATS` `ACTS` `LAW_AC` `LAW_NA`
ดึงมาจาก room datasheet ที่ทีมใช้ **ถ้า datasheet อัปเดต ต้องมาอัปเดตตรงนี้ด้วย**

### 3.2 Noise Breakout

หาระดับเสียงที่รั่วออกทางช่องลมไปถึงเพื่อนบ้าน แยก 7 ย่านความถี่แล้วรวมเป็น dB(A) ตอนท้าย
เทียบกับเกณฑ์ 10 dB(A) เหนือเสียงพื้นฐาน และเกณฑ์ผนังไม่แย่ลง
เพิ่มชิ้นส่วนเองได้ · มีตารางเป้าหมาย NC รายประเภทห้องในแท็บกฎหมาย

### 3.3 ERV Load Impact

หาว่าลม ERV ที่เติมเข้าห้องปรับอากาศกินกำลังแอร์เดิมกี่ RT และทำให้ห้องเปลี่ยนกี่องศา กี่ %RH
แยกภาระสัมผัสกับภาระแฝง มีแผนภูมิไซโครเมตริกและกราฟความไวต่อปริมาณลม

**สองตัวเลือกที่เปลี่ยนคำตอบ**
- **ลม ERV ต่อเข้าทางไหน** — เข้าท่อลมกลับใช้ `Qac + Qoa` เป็นตัวหาร จ่ายเข้าห้องตรงใช้ `Qac` อย่างเดียว
- **ความหนาแน่นลม** — ลมมาตรฐาน 1.200 (ค่าตั้งต้น ตรงกับไฟล์ทีม) หรือความหนาแน่นจริงที่หน้ากาก
  ต่างกันราว 10%

### 3.4 ERV Energy Saving

เทียบค่าไฟและเงินลงทุนระหว่างการใช้ ERV กับการใช้พัดลมระบายอากาศธรรมดา
**คิดต่อโปรเจค** ลมที่ใช้คือลมรวมของทุกห้อง ซึ่งหน้านี้บวกมาให้จากทะเบียน

- ประหยัดสุทธิ = ค่าไฟแอร์ที่ลดได้ **หักด้วย** ค่าไฟพัดลมที่เพิ่มขึ้น เพราะ ERV มีสองพัดลมและความดันสูงกว่า
- ค่าไฟแยกเป็น **แอร์** กับ **ระบบระบายอากาศ** ทั้งสองเคส
- กราฟคืนทุนเป็นจำนวนปีเทียบกับติดพัดลมธรรมดา · จุดส้มคือจุดปัจจุบัน
- **ราคา ERV และราคาพัดลมตั้งต้นเป็น 0** ไม่มีราคากลางที่อ้างได้ ไม่ใส่ก็ใช้ได้ แต่จะไม่มีตัวเลขคืนทุน
- **ราคาต่อ RT ที่ประหยัดจากการลดขนาดแอร์ก็ตั้งต้นเป็น 0** เพราะส่วนลดนั้นเกิดเฉพาะตอนที่ยังไม่ได้สั่งเครื่อง
- แท็บสไลด์คือหน้าเดียวจบสำหรับเสนอลูกค้า พิมพ์เป็นแนวนอนได้
- ตารางเครื่อง Zehnder ComfoAir มาจากแคตตาล็อกที่ทีมขายอยู่ · **ηt ไม่ได้ใส่มาให้**
  เพราะแคตตาล็อกมีหลายแถวประสิทธิภาพที่ไม่ระบุว่าแถวไหนคืออะไร ต้องเปิดสเปกรุ่นจริงแล้วกรอกเอง
- สมการกำลังพัดลม `W = Q × Δp ÷ η` ทวนกับตัวอย่างที่ Kruger คำนวณไว้เอง ต่างกัน 0.9%

### 3.5 โครงข้อมูลของทะเบียนกลาง

คีย์เดียว `coral.projects.v1` ใน `localStorage` · ทุกหน้าอยู่โดเมนและพาธเดียวกันจึงเห็นค่าเดียวกัน

```
{ projects: [ project ] }

project { id, name, client, note, off, rooms[], roi{} }
room    { id, name, w, l, h, pz, cap, qac, vent{}, erv{}, ac{} }

cap   ขนาดแอร์เดิมรวมทั้งห้อง RT      ห้องหนึ่งมีแอร์กี่ชุดก็ได้ ใส่เป็นยอดรวมก้อนเดียว
qac   ลมจ่ายของแอร์รวม CMH            ตามที่ตกลงกับทีมว่า BTU และ Cal รวมเป็นก้อนเดียวได้
vent{} ผลจาก Ventilation & CO2        { q, rec, co2, ok, cat, dur }
erv{}  ผลจาก ERV Load Impact          { rt, dT, dRH, ok, oa, qac, cap, ... }
ac{}   ผลจาก Noise Breakout           { dba, lim, ok, band[7], + ทุกช่องอินพุต }
roi{}  ผลจาก ERV Energy Saving        อินพุตทุกช่อง + roi._out ที่ Hub อ่านไปแสดง
```

**กติกาที่ต้องรักษาไว้ถ้าจะแก้ต่อ**

1. **เครื่องมือแตะเฉพาะช่องของตัวเอง** ห้ามเขียนทับ slot ของเครื่องมืออื่น
2. **เปิดเครื่องมือโดยไม่มี `?p=` และ `?r=` ต้องทำงานเหมือนเดิมทุกอย่าง** ไม่ผูกกับโปรเจคใด
   ทุกตัวทดสอบกรณีนี้แล้ว อย่าทำให้เสีย
3. **Hub เป็นเจ้าของขนาดห้อง จำนวนคน และแอร์เดิม** เครื่องมือดึงไปใช้และเขียนกลับ ไม่ใช่เก็บสำเนาของตัวเอง
4. **ลมของห้องไหลจาก CO₂ ไปหาตัวอื่น** `room.vent.q` เป็นต้นทาง ถ้าเครื่องมือปลายทางใช้ค่าอื่น
   ต้อง**บอกผู้ใช้ว่าต่างกันเท่าไร** พร้อมปุ่มดึงกลับ ไม่ใช่เงียบ ๆ ทับ
5. **การลบโปรเจคลบห้องและผลทั้งหมดไปด้วย** ที่ไหนก็ตามที่ลบได้ ต้องบอกจำนวนห้องก่อนถาม
6. **Import CSV ห้ามแทนที่รายการทั้งหมด** ของ ervroi เป็นไฟล์ฝั่ง ROI ล้วน ถ้าแทนที่จะพาห้องหายไปด้วย
   ปัจจุบันแก้ให้ merge ตามชื่อแล้ว
7. `roi._out` เป็นค่าที่คำนวณไว้ให้ Hub อ่าน **คำนวณใหม่เฉพาะโปรเจคที่เปิดอยู่ตอนพิมพ์**
   ตัวอื่นรีเฟรชตอนโหลดและตอน import ผ่าน `refreshAllOut()` เพราะการคำนวณของโปรเจคอื่นยืมช่องอินพุตไปใช้ชั่วคราว

---

## 4. ⚠️ ผลตรวจไฟล์ Cooling load Recheck ของทีม

ไฟล์ `26-xx_Cooling load Recheck.xlsm` (โครงการ Udon airport DOA Building A)
**ผลตรวจฉบับเต็มอยู่ในแท็บที่สามของ ERV Load Impact Calculator**

### ที่ผิดและควรแก้

**เซลล์ I26 คอลัมน์ Sensible ใช้ตัวแปรผิด**

```
เดิม  =((1.23*($P$12/3.6)*(B26-G26))/1000)/3.517      B26-G26 = Toa - Tsa
ใหม่  =((1.23*($P$12/3.6)*(G26-H26))/1000)/3.517      G26-H26 = Tsa - Tra
```

`Toa − Tsa` คืออุณหภูมิที่ลดลง**ภายในแกน ERV** ซึ่งเป็นความร้อนที่**ดึงคืนไว้ได้**
แต่ภาระที่มาถึงแอร์คือ `Tsa − Tra` · คอลัมน์ J26 (`=K26−I26`) ผิดตามโดยอัตโนมัติ
เมื่อแก้ I26 แล้ว J26 จะถูกเอง ไม่ต้องแก้

| ที่เงื่อนไข Udon | ในไฟล์ | ที่ถูก |
|---|---|---|
| ภาระรวม | 5.00 RT | 5.00 RT ✔ |
| ภาระสัมผัส | 2.652 | **1.141** |
| ภาระแฝง | 2.348 | **3.859** |
| SHR | 0.53 | **0.23** |

**ตัวเลขพาดหัวที่เอาไปคุยกับลูกค้าไม่ผิด** เพราะ D19 ดึงจากคอลัมน์ Total ซึ่งถูก
แต่ **ห้ามเอา SHR จากตารางนี้ไปเลือกคอยล์** จะได้คอยล์ลดความชื้นไม่พอ

### ที่ตรวจแล้วถูก

ค่าคงที่ 1.2 และ 1.23 ตรงกับ ASHRAE 2021 บทที่ 18 สมการ (7) และ (9) ·
add-in `psych()` ถูกต้อง (ต่างจากที่คำนวณใหม่ไม่เกิน 0.011%) ·
สูตร Tsa และ hsa ถูก · Tmix ถูกสำหรับกรณีต่อเข้าลมกลับ

### ที่ควรจัดการ แต่ไม่ใช่สูตรผิด

1. ตาราง 12 เดือนใส่ค่าเดียวกันหมด `MAX()` จึงไม่ได้ทำอะไร
2. ลิงก์ภูมิอากาศชี้ **พัทยา** แต่โครงการอยู่ **อุดรธานี**
3. 38 °C คู่กับ RH 60% เผื่อหนักเกิน สองค่าสุดขั้วไม่เกิดพร้อมกัน — ควรใช้คู่ DB/MCWB
   จาก ASHRAE Fundamentals บทที่ 14 Climatic Design Information
4. ลม 3,000 CMH คิดที่ความหนาแน่นลมมาตรฐาน ภาระจึงสูงกว่าจริงราว 10% (5.00 แทน 4.54 RT)
   ไม่ใช่ความผิด และเอียงฝั่งปลอดภัย แต่ควรรู้ก่อนตัดสินใจเพิ่มขนาดเครื่อง
5. เกณฑ์ 1 °C และ 20% เป็นเกณฑ์ภายในของทีม ควรเขียนกำกับว่าไม่ใช่ข้อกำหนดของมาตรฐานใด
6. D17 ในชีตงานจริงเขียนทับเป็น `=1.7*22000` ควรใส่หมายเหตุว่า 1.7 กับ 22000 คืออะไร

---

## 5. สถานะความน่าเชื่อถือของตัวเลข

### CO₂ — เชื่อถือได้
สูตรตรงกับ room datasheet ทุกหลักทศนิยม (ตรวจกับ AK6–AK11, Q6, Q9, AA, AQ11 · ส่วนต่าง = 0)
เส้นอ้างอิงในกราฟทุกเส้นระบุที่มาและระดับความมั่นใจ ตรวจกับ ASHRAE Position Document
on Indoor Carbon Dioxide แล้ว

### ภาระความร้อนจาก ERV — เชื่อถือได้ ตรวจสามทาง
1. `pws` ตรงกับตารางที่ 3 ของ ASHRAE บทที่ 1 ทุกหลักที่หนังสือพิมพ์
2. เอนทัลปีและ W ต่างจาก add-in `psych()` ของทีมไม่เกิน 0.011%
3. **โจทย์ตัวอย่าง 14–6 ของ Cengel ทั้งข้อ** — หนังสือ 511 kJ/min คำนวณได้ 510.7 (−0.1%)
   และเอกลักษณ์การผสมลมสมการ 14–24 ปิดที่ 12.736066 เท่ากันทั้งสามทาง

Cengel หาคำตอบด้วยวิธีคนละอย่าง (อ่านแผนภูมิแล้วปิดสมดุล) จึงเป็นการตรวจจริง ไม่ใช่ยืนยันซ้ำ

### เสียง — ⚠️ ยังต้องระวัง
- **acoustic louver** ใช้ค่าจริงจากแคตตาล็อก IAC Slimshield SL-300 (TL `6/7/10/12/18/18/14`, free area 45%)
- **ท่อลมบุฉนวน** คำนวณตาม SMACNA สมการ 11-102 + ตาราง 14-51 และ 14-50
- **ข้องอ และ silencer หลัก** คำนวณจากตาราง SMACNA 14-59 และ 14-62 ให้อัตโนมัติแล้ว
  รวมทั้ง regenerated sound power ของ silencer
- **end reflection ที่ปลายท่อ** ใช้ AHRI 885-2008 ภาคผนวก D ตาราง D13
- **เหลือ 3 ชิ้นที่ยังเป็นค่ากลางทั่วไป** — ตัวเครื่อง ERV · silencer ตัวที่สอง · ห้องลดเสียง
  ติดป้ายไว้ในเครื่องมือแล้ว
- **ค่าทุกช่องแก้ได้** เมื่อเลือกรุ่นจริงให้พิมพ์ทับจากแคตตาล็อก
- **ต้องให้วิศวกรอะคูสติกทวนสอบก่อนเซ็นแบบก่อสร้าง**

---

## 6. วิธีดูแลเครื่องมือ

### สิทธิ์เข้าถึง
เครื่องที่ใช้ทำงานมี Git Credential Manager ผูกบัญชี `nchatupongtrakul-ops` แล้ว push ได้เลย
**แต่ไม่มี GitHub CLI** → สร้าง repo ใหม่จากคอมมานด์ไลน์ไม่ได้ ต้องสร้างผ่านหน้าเว็บ github.com ก่อน

### แก้เครื่องมือ

```bash
git clone https://github.com/nchatupongtrakul-ops/design-management-dashboard.git
git pull                       # สำคัญ มี commit อัตโนมัติเข้ามาเรื่อย ๆ
# แก้ hub/ ventilation/ ervload/ acoustic/ ervroi/ หรือ guide/ index.html
git add . && git commit -m "..." && git push origin main
```

**ทดสอบก่อน push** เปิดเซิร์ฟเวอร์ในโฟลเดอร์ repo แล้วไล่ทั้งสาย ไม่ใช่เปิดไฟล์ตรง ๆ
เพราะทะเบียนกลางผูกกับ origin ถ้าเปิดแบบ `file://` แต่ละหน้าจะมองไม่เห็นกัน

```bash
python -m http.server 8731     # แล้วเปิด http://localhost:8731/hub/
```

สิ่งที่ต้องลองทุกครั้งที่แตะเรื่องทะเบียน คือ สร้างโปรเจคสองห้อง ไล่ทั้งสี่เครื่องมือ
กลับมาดูว่า Hub บวกถูก **แล้วเปิดเครื่องมือแบบไม่มี `?p=` ดูว่ายังทำงานเหมือนเดิม**

ทุกตัวเป็น **HTML ไฟล์เดียวจบ** ไม่มี CDN ไม่มี dependency ภายนอก
GitHub Pages ใช้เวลาราว **40–60 วินาที** · ยืนยันด้วย `curl` ก่อนบอกใครว่าขึ้นแล้ว

> **บน Windows อย่า clone ลงพาธยาว** จะเจอ `Filename too long` — ให้ clone ลง `C:\tmp\`
>
> ถ้าเทียบขนาดไฟล์ที่ live กับไฟล์ในเครื่องแล้วไม่เท่ากัน มักเป็นแค่ git แปลง LF เป็น CRLF
> ให้ `tr -d '\r'` ก่อนเทียบ อย่าเพิ่งสรุปว่าเนื้อหาต่าง

### เพิ่ม Tool ใหม่เข้า portal
ในไฟล์ `erv-team-portal/index.html` หา `const TOOLS` แล้วเติมรายการ
เมนู iframe และลิงก์ Open full page ผูกกับ registry นี้ทั้งหมด **ไม่ต้องแก้ที่อื่นเลย**

```js
{ id:'erv-load', group:'IAQ Tool',
  label:'ERV Load Impact Calculator',
  desc:'...', url:'https://.../ervload/' }
```

`group` ชื่อใหม่ = สร้างหมวดใหม่ในเมนูอัตโนมัติ

### เรื่อง build.py
`ervload` `ervroi` `hub` `guide` เคยประกอบจากหลายไฟล์ด้วย `build.py` ตอนสร้างครั้งแรก
**แต่ไฟล์ parts อยู่ใน scratchpad ของ session ไม่ได้อยู่ใน repo และตอนนี้ `index.html` ที่ live
เดินหน้าไปไกลกว่า parts แล้ว** ให้ถือว่า `index.html` คือต้นฉบับเพียงอันเดียว แก้ตรงนั้นได้เลย
อย่าไปรัน `build.py` เก่าทับ จะย้อนกลับไปเวอร์ชันก่อนหน้าหลายรอบ

ทุกหน้าเป็น **HTML ไฟล์เดียวจบ** ไม่มี CDN ไม่มี dependency ภายนอก

---

## 7. งานถัดไป

| # | งาน | ทำไม |
|---|---|---|
| 1 | **แก้เซลล์ I26 ในไฟล์ Cooling load Recheck** | ไฟล์นี้เวียนใช้หลายโครงการ ยิ่งช้ายิ่งกระจาย · **ยังไม่ได้แก้** |
| 2 | ขอ insertion loss แยก octave band จากผู้ผลิต | เหลือ 3 ชิ้นที่ยังติดป้ายประมาณการ คือ **ตัวเครื่อง ERV · Silencer ตัวที่สอง · ห้องลดเสียง** · ท่อลมบุฉนวน ข้องอ และ Silencer หลัก คำนวณตาม SMACNA ให้แล้วอัตโนมัติ — **ให้ผลกับงานจริงมากที่สุด** |
| 3 | ให้วิศวกรอะคูสติกทวนสอบชุดลดเสียง | ตัวเลขเสียงยังใช้เซ็นแบบไม่ได้ |
| 4 | ใส่ insertion loss ของ flex duct จาก AHRI 885 | ดึงข้อมูลไว้แล้ว ยังไม่ได้ต่อเข้าเครื่องมือ · end reflection ใส่ไปแล้ว |
| 5 | ขอ ηt จริงของรุ่น Zehnder ที่ทีมขาย | ROI ทั้งหน้าแขวนอยู่กับตัวเลขนี้ ตอนนี้ผู้ใช้ต้องกรอกเอง |
| 6 | พิจารณาทำเครื่องมือตัวถัดไป · NC ในห้องจากหัวจ่าย | Price catalogue มีข้อมูล NC ของหัวจ่ายครบทุกรุ่น |
| 7 | คิดเรื่องสำรองข้อมูลทะเบียน | ตอนนี้อยู่ใน `localStorage` ของเครื่องเดียว ล้างเบราว์เซอร์แล้วหาย · Export CSV คือทางเดียวที่มี |

### ที่ปิดไปแล้วตั้งแต่ฉบับก่อน

- ประกาศ กก.สวล. **ฉบับที่ 29 (พ.ศ. 2550) ยกเลิกฉบับที่ 17** — ตรวจตัวบทแล้ว อ้างในสัญญาได้
- เกณฑ์ CO₂ **900 ppm ของ WELL v2 อยู่ที่ A06 Part 1 Option 2** ไม่ใช่ A08 และไม่ใช่ A03
- AHRI 885-2008 appendix D table D13 **end reflection** ใส่ในเครื่องมือแล้ว
- SMACNA ตาราง silencer และข้องอ ใส่ครบแล้ว ทั้ง insertion loss และ regenerated sound power

### ถ้ากลับมาทำ Music Hall ต่อ
ยังต้องสำรวจเพิ่ม 6 รายการก่อนเสนอราคา โดยตัวที่สำคัญที่สุดคือ
**CO₂ decay test หลังคนออกหมด** (ได้อัตรารั่วซึมของอาคารเดิม) และ
**วัดสเปกตรัมเสียงแยกย่าน** (ถ้าไม่มีจะเลือก silencer ได้แค่เดา)

ตัวแปรที่อันตรายที่สุดคือ**ลักษณะกิจกรรม** — ทุกตัวเลขคำนวณบนสมมติฐาน Seated, quiet
ถ้าเป็นยืน +17% ถ้าเต้น **+258%** → ข้อเสนอต้องผูกค่าที่รับประกันเข้ากับรูปแบบงานในสัญญา

---

## 8. ⚠️ ค่าที่เคยผิดและแก้แล้ว — อย่าใช้ค่าเก่า

| เรื่อง | เคยเขียนผิด | ค่าที่ถูก |
|---|---|---|
| อัตราระบายอากาศตามกฎกระทรวง | ~4 ACH | ตารางห้องปรับอากาศเป็น **CMH/ตร.ม.** ไม่ใช่ ACH · ไนท์คลับ = 10 → 2,930 CMH |
| อัตราปล่อย CO₂ ต่อคน | 0.020 m³/h | **0.01599 m³/h** ตาม ASHRAE จริง |
| เสียงที่ 10 เมตร | 32 dB(A) | **26–29 dB(A)** ขึ้นกับ louver ที่ใช้ |
| โหลดทำความเย็น Music Hall | 24.7 → 7.4 ตัน | ช่วง **22.6–24.4 → 5.7–8.5** ประหยัด 14.7–18.3 ตัน |
| ความดันตกคร่อม louver | 25–40 Pa ที่ 2 ม./วิ | **46 Pa** ตามแคตตาล็อก IAC SL-300 |
| ชื่อประกาศเสียงรบกวน | ประกาศคณะกรรมการควบคุมมลพิษ | **ประกาศคณะกรรมการสิ่งแวดล้อมแห่งชาติ ฉบับที่ 29** |
| WELL feature 900 ppm | A08 · แล้วเดาว่า A03 หรือ A06 | **A06 Part 1 Option 2** ของ WELL v2 · ตรวจตัวบทแล้ว ยุติ |
| ที่เก็บข้อมูลของเครื่องมือ | "ปิดหน้าแล้วค่าหาย" | มีทะเบียนกลางแล้ว ค่าอยู่ต่อ · แต่**อยู่ในเบราว์เซอร์เครื่องเดียว** ไม่ได้ขึ้นเซิร์ฟเวอร์ |

---

*เอกสารนี้สรุปงานถึง 7 กันยายน 2569 · ตัวเลขทุกตัวตรวจซ้ำได้จากแท็บที่มาในเครื่องมือแต่ละตัว
ค่าที่ติดป้ายว่าประมาณการ ต้องแทนที่ด้วยข้อมูลผู้ผลิตจริงก่อนใช้ออกแบบก่อสร้าง*
