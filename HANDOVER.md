# Handover — เครื่องมือวิศวกรรม IAQ · Acoustic · ERV Load

อัปเดต 26 สิงหาคม 2569 · เอกสารนี้อยู่ใน repo เพื่อให้เข้าถึงได้โดยไม่ผูกกับบัญชีใด

> **คนที่จะ *ใช้* เครื่องมือ ให้เปิด [คู่มือการใช้งาน](https://nchatupongtrakul-ops.github.io/design-management-dashboard/guide/) แทน**
> เอกสารนี้เขียนสำหรับคนที่จะมา *ดูแลหรือแก้* เครื่องมือต่อ

---

## 1. สถานะโดยสรุป

งานมีสองส่วน คือ **เครื่องมือคำนวณที่ขึ้นเว็บแล้ว** (ใช้งานได้ทันที) และ
**งานออกแบบห้องสัมมนาดนตรี** (พักไว้ตามที่ตกลง)

| | สถานะ |
|---|---|
| Ventilation & CO₂ Calculator | ออนไลน์ |
| Noise Breakout Calculator | ออนไลน์ · **ค่าลดเสียง 5 ชิ้นยังเป็นค่าประมาณ** |
| ERV Load Impact Calculator | ออนไลน์ |
| คู่มือการใช้งาน | ออนไลน์ |
| ERV Team Portal (เมนู Tools) | ออนไลน์ · ลงทะเบียนครบทั้งสามตัว |
| ข้อเสนอ ERV ห้อง Music Hall | ร่างเสร็จ · **พักไว้** ตามที่ตกลงว่าจะเน้นเครื่องมือก่อน |

---

## 2. ที่อยู่ของทุกอย่าง

| สิ่งที่ทำ | URL | Repo · โฟลเดอร์ |
|---|---|---|
| ERV Team Portal | https://nchatupongtrakul-ops.github.io/erv-team-portal/ | `erv-team-portal` · root |
| **คู่มือการใช้งาน** | https://nchatupongtrakul-ops.github.io/design-management-dashboard/guide/ | `design-management-dashboard` · `guide/` |
| Ventilation & CO₂ | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ventilation/ | `design-management-dashboard` · `ventilation/` |
| Noise Breakout | https://nchatupongtrakul-ops.github.io/design-management-dashboard/acoustic/ | `design-management-dashboard` · `acoustic/` |
| ERV Load Impact | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ervload/ | `design-management-dashboard` · `ervload/` |
| Design Management Dashboard | https://nchatupongtrakul-ops.github.io/design-management-dashboard/ | ของเดิม · **ไม่ได้แก้ และห้ามแก้** |
| Construction Dashboard | ของทีมอื่น | `wyuwachotiphan-crypto/CM-Dashborad` · **ไม่ได้แตะ** |

> repo `design-management-dashboard` มี commit `Update dashboard data` เข้ามาอัตโนมัติเป็นระยะ
> แตะเฉพาะ `data.json` — **ให้ `git pull` ก่อน push ทุกครั้ง**

---

## 3. เครื่องมือแต่ละตัวตอบอะไร

สามตัวต่อกันเป็นสายเดียว ผลของตัวก่อนหน้าเป็นอินพุตของตัวถัดไป

```
1  Ventilation & CO2   →  ปริมาณลมที่ต้องเติม เป็น CMH
2  ERV Load Impact     →  ผลกระทบต่อแอร์เดิม เป็น RT และ องศา
3  Noise Breakout      →  ชุดลดเสียงที่ต้องมี

ถ้า 2 หรือ 3 ไม่ผ่าน  →  ย้อนกลับไป 1 ลดลมลง แล้วไล่ใหม่
```

### 3.1 Ventilation & CO₂

หาปริมาณลมบริสุทธิ์ที่ต้องเติม และทำนาย CO₂ ตลอดระยะเวลาใช้ห้อง
เทียบกับเกณฑ์กฎกระทรวงฉบับที่ 39 · ASHRAE 62.1 · LEED/WELL พร้อมกัน
มีเส้นเปรียบเทียบระบบเดิมของโครงการ และช่องย้อนกลับ (ตั้งเป้า ppm แล้วหาลม)

**ตารางข้อมูลที่ฝังไว้** — ASHRAE 62.1 Table 6-1 จำนวน 108 หมวด · อัตราปล่อย CO₂ 32 กิจกรรม ·
กฎกระทรวงทั้งสองชุด อยู่ในตัวแปร `CATS` `ACTS` `LAW_AC` `LAW_NA`
ดึงมาจาก room datasheet ที่ทีมใช้ **ถ้า datasheet อัปเดต ต้องมาอัปเดตตรงนี้ด้วย**

### 3.2 Noise Breakout

หาระดับเสียงที่รั่วออกทางช่องลมไปถึงเพื่อนบ้าน แยก 7 ย่านความถี่แล้วรวมเป็น dB(A) ตอนท้าย
เทียบกับเกณฑ์ 10 dB(A) เหนือเสียงพื้นฐาน และเกณฑ์ผนังไม่แย่ลง
เพิ่มชิ้นส่วนเองได้ · มีตารางเป้าหมาย NC รายประเภทห้องในแท็บกฎหมาย

### 3.3 ERV Load Impact — ตัวใหม่ล่าสุด

หาว่าลม ERV ที่เติมเข้าห้องปรับอากาศกินกำลังแอร์เดิมกี่ RT และทำให้ห้องเปลี่ยนกี่องศา กี่ %RH
แยกภาระสัมผัสกับภาระแฝง มีแผนภูมิไซโครเมตริกและกราฟความไวต่อปริมาณลม

**สองตัวเลือกที่เปลี่ยนคำตอบ**
- **ลม ERV ต่อเข้าทางไหน** — เข้าท่อลมกลับใช้ `Qac + Qoa` เป็นตัวหาร จ่ายเข้าห้องตรงใช้ `Qac` อย่างเดียว
- **ความหนาแน่นลม** — ลมมาตรฐาน 1.200 (ค่าตั้งต้น ตรงกับไฟล์ทีม) หรือความหนาแน่นจริงที่หน้ากาก
  ต่างกันราว 10%

---

## 4. ⚠️ ผลตรวจไฟล์ Cooling load Recheck ของทีม

ไฟล์ `26-xx_Cooling load Recheck.xlsm` (โครงการ Udon airport DOA Building A)
**ผลตรวจฉบับเต็มอยู่ในแท็บที่สามของ ERV Load Impact Calculator**

### ที่ผิดและควรแก้

**เซลล์ I26 คอลัมน์ Sensible ใช้ตัวแปรผิด**

```
เดิม  =((1.23*($P$12/3.6)*(B26-G26))/1000)/3.517      B26-G26 = Toa - Tsa
ใหม่  =((1.23*($P$12/3.6)*(G26-H26))/1000)/3.517      G26-H26 = Tsa - Tra
```

`Toa − Tsa` คืออุณหภูมิที่ลดลง**ภายในแกน ERV** ซึ่งเป็นความร้อนที่**ดึงคืนไว้ได้**
แต่ภาระที่มาถึงแอร์คือ `Tsa − Tra` · คอลัมน์ J26 (`=K26−I26`) ผิดตามโดยอัตโนมัติ
เมื่อแก้ I26 แล้ว J26 จะถูกเอง ไม่ต้องแก้

| ที่เงื่อนไข Udon | ในไฟล์ | ที่ถูก |
|---|---|---|
| ภาระรวม | 5.00 RT | 5.00 RT ✔ |
| ภาระสัมผัส | 2.652 | **1.141** |
| ภาระแฝง | 2.348 | **3.859** |
| SHR | 0.53 | **0.23** |

**ตัวเลขพาดหัวที่เอาไปคุยกับลูกค้าไม่ผิด** เพราะ D19 ดึงจากคอลัมน์ Total ซึ่งถูก
แต่ **ห้ามเอา SHR จากตารางนี้ไปเลือกคอยล์** จะได้คอยล์ลดความชื้นไม่พอ

### ที่ตรวจแล้วถูก

ค่าคงที่ 1.2 และ 1.23 ตรงกับ ASHRAE 2021 บทที่ 18 สมการ (7) และ (9) ·
add-in `psych()` ถูกต้อง (ต่างจากที่คำนวณใหม่ไม่เกิน 0.011%) ·
สูตร Tsa และ hsa ถูก · Tmix ถูกสำหรับกรณีต่อเข้าลมกลับ

### ที่ควรจัดการ แต่ไม่ใช่สูตรผิด

1. ตาราง 12 เดือนใส่ค่าเดียวกันหมด `MAX()` จึงไม่ได้ทำอะไร
2. ลิงก์ภูมิอากาศชี้ **พัทยา** แต่โครงการอยู่ **อุดรธานี**
3. 38 °C คู่กับ RH 60% เผื่อหนักเกิน สองค่าสุดขั้วไม่เกิดพร้อมกัน — ควรใช้คู่ DB/MCWB
   จาก ASHRAE Fundamentals บทที่ 14 Climatic Design Information
4. ลม 3,000 CMH คิดที่ความหนาแน่นลมมาตรฐาน ภาระจึงสูงกว่าจริงราว 10% (5.00 แทน 4.54 RT)
   ไม่ใช่ความผิด และเอียงฝั่งปลอดภัย แต่ควรรู้ก่อนตัดสินใจเพิ่มขนาดเครื่อง
5. เกณฑ์ 1 °C และ 20% เป็นเกณฑ์ภายในของทีม ควรเขียนกำกับว่าไม่ใช่ข้อกำหนดของมาตรฐานใด
6. D17 ในชีตงานจริงเขียนทับเป็น `=1.7*22000` ควรใส่หมายเหตุว่า 1.7 กับ 22000 คืออะไร

---

## 5. สถานะความน่าเชื่อถือของตัวเลข

### CO₂ — เชื่อถือได้
สูตรตรงกับ room datasheet ทุกหลักทศนิยม (ตรวจกับ AK6–AK11, Q6, Q9, AA, AQ11 · ส่วนต่าง = 0)
เส้นอ้างอิงในกราฟทุกเส้นระบุที่มาและระดับความมั่นใจ ตรวจกับ ASHRAE Position Document
on Indoor Carbon Dioxide แล้ว

### ภาระความร้อนจาก ERV — เชื่อถือได้ ตรวจสามทาง
1. `pws` ตรงกับตารางที่ 3 ของ ASHRAE บทที่ 1 ทุกหลักที่หนังสือพิมพ์
2. เอนทัลปีและ W ต่างจาก add-in `psych()` ของทีมไม่เกิน 0.011%
3. **โจทย์ตัวอย่าง 14–6 ของ Cengel ทั้งข้อ** — หนังสือ 511 kJ/min คำนวณได้ 510.7 (−0.1%)
   และเอกลักษณ์การผสมลมสมการ 14–24 ปิดที่ 12.736066 เท่ากันทั้งสามทาง

Cengel หาคำตอบด้วยวิธีคนละอย่าง (อ่านแผนภูมิแล้วปิดสมดุล) จึงเป็นการตรวจจริง ไม่ใช่ยืนยันซ้ำ

### เสียง — ⚠️ ยังต้องระวัง
- **acoustic louver** ใช้ค่าจริงจากแคตตาล็อก IAC Slimshield SL-300 (TL `6/7/10/12/18/18/14`, free area 45%)
- **ท่อลมบุฉนวน** คำนวณตาม SMACNA สมการ 11-102 + ตาราง 14-51 และ 14-50
- **อีก 5 ชิ้นเป็นค่ากลางทั่วไป** — silencer หลัก · silencer ตัวที่สอง · ข้องอ · ตัวเครื่อง ERV · ห้องลดเสียง
  ติดป้ายแดงไว้ในเครื่องมือแล้ว
- **ค่าทุกช่องแก้ได้** เมื่อเลือกรุ่นจริงให้พิมพ์ทับจากแคตตาล็อก
- **ต้องให้วิศวกรอะคูสติกทวนสอบก่อนเซ็นแบบก่อสร้าง**

---

## 6. วิธีดูแลเครื่องมือ

### สิทธิ์เข้าถึง
เครื่องที่ใช้ทำงานมี Git Credential Manager ผูกบัญชี `nchatupongtrakul-ops` แล้ว push ได้เลย
**แต่ไม่มี GitHub CLI** → สร้าง repo ใหม่จากคอมมานด์ไลน์ไม่ได้ ต้องสร้างผ่านหน้าเว็บ github.com ก่อน

### แก้เครื่องมือ

```bash
git clone https://github.com/nchatupongtrakul-ops/design-management-dashboard.git
git pull                       # สำคัญ มี commit อัตโนมัติเข้ามาเรื่อย ๆ
# แก้ ventilation/ acoustic/ ervload/ หรือ guide/ index.html
git add . && git commit -m "..." && git push origin main
```

ทุกตัวเป็น **HTML ไฟล์เดียวจบ** ไม่มี CDN ไม่มี dependency ภายนอก
GitHub Pages ใช้เวลาราว **40–60 วินาที** · ยืนยันด้วย `curl` ก่อนบอกใครว่าขึ้นแล้ว

> **บน Windows อย่า clone ลงพาธยาว** จะเจอ `Filename too long` — ให้ clone ลง `C:\tmp\`
>
> ถ้าเทียบขนาดไฟล์ที่ live กับไฟล์ในเครื่องแล้วไม่เท่ากัน มักเป็นแค่ git แปลง LF เป็น CRLF
> ให้ `tr -d '\r'` ก่อนเทียบ อย่าเพิ่งสรุปว่าเนื้อหาต่าง

### เพิ่ม Tool ใหม่เข้า portal
ในไฟล์ `erv-team-portal/index.html` หา `const TOOLS` แล้วเติมรายการ
เมนู iframe และลิงก์ Open full page ผูกกับ registry นี้ทั้งหมด **ไม่ต้องแก้ที่อื่นเลย**

```js
{ id:'erv-load', group:'IAQ Tool',
  label:'ERV Load Impact Calculator',
  desc:'...', url:'https://.../ervload/' }
```

`group` ชื่อใหม่ = สร้างหมวดใหม่ในเมนูอัตโนมัติ

### โครงสร้างของ ervload
ตัวนี้ประกอบจากหลายไฟล์ด้วย `build.py` ต่างจากสองตัวแรกที่เป็นไฟล์เดียวตรง ๆ
ถ้าจะแก้เยอะให้แก้ที่ `head.html` `style.css` `body1-3.html` `app.js` แล้วรัน `build.py`
ถ้าแก้นิดเดียวแก้ที่ `index.html` ตรง ๆ ก็ได้ **แต่อย่าลืมย้อนกลับไปแก้ที่ parts ด้วย ไม่งั้น build ครั้งหน้าจะทับ**
(ไฟล์ parts อยู่ใน scratchpad ของ session ไม่ได้อยู่ใน repo — ถ้าหายให้แก้ `index.html` ตรง ๆ ต่อไปเลย)

---

## 7. งานถัดไป

| # | งาน | ทำไม |
|---|---|---|
| 1 | **แก้เซลล์ I26 ในไฟล์ Cooling load Recheck** | ไฟล์นี้เวียนใช้หลายโครงการ ยิ่งช้ายิ่งกระจาย |
| 2 | ขอ insertion loss แยก octave band จากผู้ผลิต silencer | แทนค่าประมาณ 5 ชิ้นในตัว Noise Breakout — **ให้ผลกับงานจริงมากที่สุด** |
| 3 | ให้วิศวกรอะคูสติกทวนสอบชุดลดเสียง | ตัวเลขเสียงยังใช้เซ็นแบบไม่ได้ |
| 4 | เพิ่ม end reflection และ flex duct จาก AHRI 885 | มีข้อมูลแล้วจาก Price catalogue หน้า F-21/F-22 ยังไม่ได้ใส่ |
| 5 | ตรวจตัวบทประกาศ กก.สวล. ฉบับที่ 29 ว่ายกเลิกฉบับ 17 หรือไม่ | ก่อนอ้างในสัญญา |
| 6 | พิจารณาทำเครื่องมือตัวที่สี่ · NC ในห้องจากหัวจ่าย | Price catalogue มีข้อมูล NC ของหัวจ่ายครบทุกรุ่น |

### ถ้ากลับมาทำ Music Hall ต่อ
ยังต้องสำรวจเพิ่ม 6 รายการก่อนเสนอราคา โดยตัวที่สำคัญที่สุดคือ
**CO₂ decay test หลังคนออกหมด** (ได้อัตรารั่วซึมของอาคารเดิม) และ
**วัดสเปกตรัมเสียงแยกย่าน** (ถ้าไม่มีจะเลือก silencer ได้แค่เดา)

ตัวแปรที่อันตรายที่สุดคือ**ลักษณะกิจกรรม** — ทุกตัวเลขคำนวณบนสมมติฐาน Seated, quiet
ถ้าเป็นยืน +17% ถ้าเต้น **+258%** → ข้อเสนอต้องผูกค่าที่รับประกันเข้ากับรูปแบบงานในสัญญา

---

## 8. ⚠️ ค่าที่เคยผิดและแก้แล้ว — อย่าใช้ค่าเก่า

| เรื่อง | เคยเขียนผิด | ค่าที่ถูก |
|---|---|---|
| อัตราระบายอากาศตามกฎกระทรวง | ~4 ACH | ตารางห้องปรับอากาศเป็น **CMH/ตร.ม.** ไม่ใช่ ACH · ไนท์คลับ = 10 → 2,930 CMH |
| อัตราปล่อย CO₂ ต่อคน | 0.020 m³/h | **0.01599 m³/h** ตาม ASHRAE จริง |
| เสียงที่ 10 เมตร | 32 dB(A) | **26–29 dB(A)** ขึ้นกับ louver ที่ใช้ |
| โหลดทำความเย็น Music Hall | 24.7 → 7.4 ตัน | ช่วง **22.6–24.4 → 5.7–8.5** ประหยัด 14.7–18.3 ตัน |
| ความดันตกคร่อม louver | 25–40 Pa ที่ 2 ม./วิ | **46 Pa** ตามแคตตาล็อก IAC SL-300 |
| ชื่อประกาศเสียงรบกวน | ประกาศคณะกรรมการควบคุมมลพิษ | **ประกาศคณะกรรมการสิ่งแวดล้อมแห่งชาติ ฉบับที่ 29** |
| WELL feature 900 ppm | A08 | A08 คือเรื่องติดเซ็นเซอร์ · เกณฑ์อยู่ที่ **A03 หรือ A06** (แหล่งรองขัดกัน · ยังไม่ยุติ) |

---

*เอกสารนี้สรุปงานถึง 26 สิงหาคม 2569 · ตัวเลขทุกตัวตรวจซ้ำได้จากแท็บที่มาในเครื่องมือแต่ละตัว
ค่าที่ติดป้ายว่าประมาณการ ต้องแทนที่ด้วยข้อมูลผู้ผลิตจริงก่อนใช้ออกแบบก่อสร้าง*
